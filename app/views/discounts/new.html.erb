<h2> Add a Discount </h2>

<%= render partial: 'discount_form', locals: {
                                              model: @discount, 
                                              url: merchant_discounts_path(@merchant.id)
                                              } %>
